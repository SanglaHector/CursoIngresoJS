
function mostrar()
{
    var base;

    var altura;

    var superficie;

    var perimetro;

    var lado;

    base = prompt("Ingrese la base del triangulo");

    altura = prompt("ingrese la altura del triangulo");

    base = parseInt(base);

    altura = parseInt(altura);

    lado = base

    perimetro = lado * 3

    superficie = ( base * altura ) / 2;

    alert("El perimetro es igual a " + perimetro +" y la superficie es igual a " + superficie)


}
